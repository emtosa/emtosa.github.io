title: "emtosa — Personal Blog"
description: "Personal writing on technology, AI, developer workflows, and broader community topics."
url: "https://emtosa.github.io"
baseurl: ""

author:
  name: "tosao"

# Build settings
theme: minima
markdown: kramdown
highlighter: rouge

# Plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "tosao"

# Pagination (optional, add jekyll-paginate to Gemfile to enable)
# paginate: 5
# paginate_path: "/page:num/"

permalink: /posts/:year/:month/:day/:title/

exclude:
  - README.md
  - scripts/
  - Gemfile
  - Gemfile.lock
  - .git/
